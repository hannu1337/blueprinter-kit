# Sample integration

Connects the project to the sample service.

## Removal

1. Delete `app/api/src/integrations/sample/`.
2. Delete `infra/terraform/integration-sample.tf`.
3. Remove the `sample` entry from the manifest's `integrations`.
